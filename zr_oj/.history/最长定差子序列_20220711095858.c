#include <stdio.h>
int main(){
    int length;
    scanf("%d\n",&length);
    int arr[length];
    for(int i=0;i<length;i++){
        scanf("%d\n",&arr[i]);
    }
    int difference;
    scanf("%d\n",&difference);
    if(lengh==0 || difference<0){
        printf("0\n");
        return 0;
    }

    // think:
    // 1. two sutiation: 
    return 0;
}